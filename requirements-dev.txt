-r requirements.txt

# Testing & linting (CI)
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4.0

# fastapi/httpx tested via backend/test_main.py; mcp is needed to import
# backend/mcp_server.py (mounted at /mcp in backend/main.py).
fastapi>=0.110
httpx
mcp>=1.9
