# Core ML / retrieval
sentence-transformers>=2.7.0
datasets>=2.18.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10

# Knowledge graph
python-arango>=7.9.0    # benchmarked ablation pipeline (scripts/ingest.py, RESULTS.md)
neo4j>=5.20             # hosted agent's demo graph only (scripts/ingest_neo4j.py)

# LLM client
requests>=2.31

# Plotting / reporting
matplotlib>=3.7
seaborn>=0.13
pandas>=2.0

# Notebooks / UI (optional at runtime, used by notebooks)
tqdm>=4.66

# Config
python-dotenv>=1.0
